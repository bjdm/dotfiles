# select current suggestion
bindkey '^\E[20;5~]' autosuggest-execute

# select to end of word
# Note: this is actually a zle widget, not an autosuggest command
bindkey '^@' forward-word

# Custom colours for zsh-autosuggestions
